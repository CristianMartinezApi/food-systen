services:
  # Frontend (Next.js)
  frontend:
    build:
      context: ../front-end
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_API_URL: ${DEPLOY_NEXT_PUBLIC_API_URL:-http://localhost:8001/api}
        NEXT_PUBLIC_SOCKET_URL: ${DEPLOY_NEXT_PUBLIC_SOCKET_URL:-http://localhost:8001}
    ports:
      - "3001:3000"
    volumes:
      - ../front-end/src:/app/src
      - ../front-end/app:/app/app
      - ../front-end/public:/app/public
    depends_on:
      - backend
    networks:
      - food-network
    environment:
      - NEXT_PUBLIC_API_URL=${DEPLOY_NEXT_PUBLIC_API_URL:-http://localhost:8001/api}
      - NEXT_PUBLIC_SOCKET_URL=${DEPLOY_NEXT_PUBLIC_SOCKET_URL:-http://localhost:8001}

  # Backend (Node.js/Express)
  backend:
    build:
      context: ../back-end
      dockerfile: Dockerfile
    command: sh -c "npx prisma migrate deploy && npm run dev"
    ports:
      - "8001:8000"
    volumes:
      - ../back-end/src:/app/src
      - ../back-end/prisma:/app/prisma
      - ../back-end/uploads:/app/uploads
    environment:
      - PORT=8000
      - TZ=America/Sao_Paulo
      - DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
      - JWT_SECRET=${JWT_SECRET}
      - RESEND_API_KEY=${RESEND_API_KEY}
      - ALLOWED_ORIGINS=${ALLOWED_ORIGINS:-http://localhost:3001,http://127.0.0.1:3001,http://localhost:3000,http://127.0.0.1:3000}
      - INITIAL_ADMIN_PASSWORD=${INITIAL_ADMIN_PASSWORD}
      - INITIAL_SUPERADMIN_PASSWORD=${INITIAL_SUPERADMIN_PASSWORD}
    depends_on:
      - db
    networks:
      - food-network

  # Database (PostgreSQL)
  db:
    image: postgres:15-alpine
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
    # ⚠️ CUIDADO: Porta 5432 removida em produção
    # Descomente apenas em desenvolvimento local:
    # ⚠️ CUIDADO: Porta 5432 removida em produção
    # Descomente apenas em desenvolvimento local:
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - food-network

networks:
  food-network:
    driver: bridge

volumes:
  postgres_data:
    name: food-systen_postgres_data
